<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf8" />
    <title>{{title}}</title>
    <!-- ReDoc doesn't change outer page styles -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Link to Google Fonts for custom font -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: 'Roboto', sans-serif; /* Use the custom font */
      }
    </style>
    {{{redocHead}}}
    <!-- Custom styles to slightly modify colors -->
    <style>
      :root {
        --primary-color: #000000; /* Adjust this to your preferred primary color */
        --text-color: #333333;    /* Adjust this to your preferred text color */
        --background-color: #f0f0f0; /* Light gray background color */
      }
      body {
        color: var(--text-color);
        background-color: var(--background-color); /* Apply the background color */
      }
      .menu-content {
        background-color: #f5f5f5; /* Light gray background for the menu */
      }
      .redoc-wrap .menu-content .menu-item-title {
        color: var(--primary-color);
      }
      .api-info h1 {
        color: var(--primary-color);
      }
      /* Ensure the main content area has a white background */
      .redoc-wrap .api-content {
        background-color: #ffffff;
      }
    </style>
  </head>
  <body>
    {{{redocHTML}}}
  </body>
</html>