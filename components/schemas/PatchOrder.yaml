properties:
  orderContactPersonId:
    type: integer
    description: The ID of the contact person for the order
    example: 2255
  newOrderContactPerson:
    $ref: "./NewContactPerson.yaml"
  salesPersonId:
    type: integer
    description: The ID of the sales person for the order
    example: 100
  externalOrderReference:
    type: string
    description: The external order reference
    example: "EXT-12345"
  orderStatus:
    type: string
    description: "The status to set for the order"
    enum:
      - Draft
      - Sent to customer
      - Accepted by customer
      - Active order
  trips:
    type: array
    items:
      $ref: "./PostTrip.yaml"
